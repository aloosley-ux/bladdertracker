# High-contrast theme assets

Place high-contrast-theme-specific asset files here.

Files in this directory are referenced by the asset registry
(`src/assets/assetRegistry.ts`) and automatically served when
the app is in **high-contrast** mode.

See `docs/ui-asset-integration-guide.md` for naming conventions,
supported formats, and recommended dimensions.
