The project includes `vitest-axe` for accessibility testing, but CI does not explicitly run accessibility checks. Add a job to run axe checks on key pages/components and fail CI for serious violations.

Proposed fix:
- Create a separate CI job (or step) that runs `npx vitest --run --filter accessibility` or a specific test file set that runs axe assertions.

---

## 5) Validate README/example env usage
- Labels: docs
- Severity: low

Files referenced:
- README.md (line contains an example export DATABASE_URL string)

Description:
README contains an example `export DATABASE_URL="postgres://user:pass@ep-xxx.neon.tech/dbname?sslmode=require"`. Ensure all examples use clearly fake placeholders (e.g., <DB_USER>) and remind contributors not to paste real credentials into examples or commits.

Proposed fix:
- Update README and .env.example to use `<PLACEHOLDER>` values.

---

## 6) Add issue templates for security-sensitive changes
- Labels: infra, security
- Severity: low

Description:
Repo already has SECURITY.md, but ensure PR templates and issue templates remind contributors to avoid committing personal data and provide guidance on handling GDPR-sensitive changes.

Proposed fix:
- Update `.github/ISSUE_TEMPLATE/bug_report.yml` and `feature_request.yml` with a security checklist prompt for data-handling changes.

---

## How to convert drafts into Issues
1. Copy each section into a new GitHub issue title/body.
2. Apply suggested labels and a milestone if relevant.
3. Assign to a maintainer or leave unassigned for triage.

---

Generated by automated scan performed by Copilot CLI session. For each draft, I can:
- Create GitHub issues directly (requires repo write access / PAT)
- Open a PR with an initial logger implementation or CI changes
- Or refine drafts into smaller actionable subtasks

Tell me which drafts to convert to Issues and whether to group into a single epic or create separate issues.