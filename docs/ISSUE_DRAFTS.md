# BladderTracker — Automated Review Issue Drafts

These are draft issues created from an automated scan (console logs, ESLint suppressions, missing CI checks, and documentation). Individual draft files are available in `docs/github-issues/` — convert those files into GitHub Issues using `gh issue create --body-file` or the repository's documented process, adjust labels, and assign as needed.

---

## 1) Replace console.* logging with structured logger
- Labels: bug, tech-debt
- Severity: medium

Files referenced:
- api/auth.ts:241
- src/components/ErrorBoundary.tsx:22
- src/context/AppContext.tsx:155, 358, 917

Description:
Several places use console.error/console.log for error reporting. In production this should use a structured logger (e.g., pino, bunyan, or a wrapper that can be disabled/redirected) and avoid leaking sensitive data (child identifiers, PII) to console. Centralize logging to allow configurable log levels per environment.

Proposed fix:
- Add a small logging wrapper (src/lib/logger.ts) that respects NODE_ENV and redacts sensitive fields.
- Replace console.* usages with the logger.
- Ensure serverless functions do not emit PII to stdout/stderr in production.

---

## 2) Remove ESLint disable comments and address hook deps
- Labels: bug, linter
- Severity: medium

Files referenced:
- src/context/AppContext.tsx (line with `// eslint-disable-next-line react-hooks/exhaustive-deps`)
- src/pages/AddEntryPage.tsx (same)

Description:
Two instances of disabling the `react-hooks/exhaustive-deps` rule were found. This can introduce subtle bugs when effect dependency arrays are incorrect. Investigate the reason for the disable, fix the dependency arrays or refactor the logic to avoid stale closures.

Proposed fix:
- Re-enable the lint rule and update the hook dependency arrays.
- If a dependency must be omitted, add a clear code comment explaining why and add a focused test covering the behavior.

---

## 3) Add scheduled dependency & security checks to CI
- Labels: ci, security
- Severity: low

Description:
CI currently runs install/test/lint/build. Add a weekly GitHub Action to:
- Run `npm audit --json` and create a PR or issue for high/critical vulnerabilities
- Run Dependabot or schedule `npm outdated`/`npm audit` to keep dependencies fresh

Proposed fix:
- Add a `.github/workflows/deps.yml` that runs weekly and opens automated PRs or creates issues when critical updates are available.

---

## 4) Add accessibility (axe) checks to CI
- Labels: accessibility, ci
- Severity: medium

Description:
The project includes `vitest-axe` for accessibility testing, but CI does not explicitly run accessibility checks. Add a job to run axe checks on key pages/components and fail CI for serious violations.

Proposed fix:
- Create a separate CI job (or step) that runs `npx vitest --run --filter accessibility` or a specific test file set that runs axe assertions.

---

## 5) Validate README/example env usage
- Labels: docs
- Severity: low

Files referenced:
- README.md (line contains an example export DATABASE_URL string)

Description:
README contains an example `export DATABASE_URL="postgres://user:pass@ep-xxx.neon.tech/dbname?sslmode=require"`. Ensure all examples use clearly fake placeholders (e.g., <DB_USER>) and remind contributors not to paste real credentials into examples or commits.

Proposed fix:
- Update README and .env.example to use `<PLACEHOLDER>` values.

---

## 6) Add issue templates for security-sensitive changes
- Labels: infra, security
- Severity: low

Description:
Repo already has SECURITY.md, but ensure PR templates and issue templates remind contributors to avoid committing personal data and provide guidance on handling GDPR-sensitive changes.

Proposed fix:
- Update `.github/ISSUE_TEMPLATE/bug_report.yml` and `feature_request.yml` with a security checklist prompt for data-handling changes.

---

## How to convert drafts into Issues
1. Copy each section into a new GitHub issue title/body.
2. Apply suggested labels and a milestone if relevant.
3. Assign to a maintainer or leave unassigned for triage.

---

Generated by automated scan performed by Copilot CLI session. For each draft, I can:
- Create GitHub issues directly (requires repo write access / PAT)
- Open a PR with an initial logger implementation or CI changes
- Or refine drafts into smaller actionable subtasks

Tell me which drafts to convert to Issues and whether to group into a single epic or create separate issues.